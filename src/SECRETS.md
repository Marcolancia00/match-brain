# Secrets & configuration (NOT committed)

This project uses Google Apps Script ScriptProperties for secrets.

Required keys:
- TELEGRAM_BOT_TOKEN
- TELEGRAM_CHAT_ID
- API_BASE (if using a private proxy endpoint)

Never commit tokens, chat ids, API keys, spreadsheet ids, or private endpoints to GitHub.
